package at.cb.jf;

public class Bsp {
    public static void main(String[] args) {
        int problemSize = 0;
        int threshold = 0;

        if(problemSize < threshold){
            // Problem direkt lösen
        } else {
            // Problem in kleinere Probleme zerteilen

            // Rekursives Lösen der kleineren Probleme

            // Zusammenfügen der Teillösungen
        }
    }
}
